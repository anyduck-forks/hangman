---
title: "useHangman"
---

# useHangman

**Файл:** src/hooks/useHangman.js

**Призначення:**
- Iнiцiалiзацiя гри (випадкове слово, життя, початок часу).
- Обробка спроби (перевiрка лiтери, зменшення життя, перевiрка перемоги/поразки).
- Пiдготовка `wordProgress` (вiдкритi лiтери).
- За потреби рахує iнформацiйнi метрики (ентропiя) для аналiтики.

**Повертає:**
- `gameState` — слово, спроби, залишок життiв, час старту.
- `analysis` — кандидати та ентропiї лiтер.
- `guessLetter(letter)` — виконати спробу.
- `wordProgress` — масив лiтер/`null`.
